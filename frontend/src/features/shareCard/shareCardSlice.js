import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const shareCardSlice = createSlice({
	name: 'shareCard',
	initialState,
	reducers: {},
});

export default shareCardSlice.reducer;
